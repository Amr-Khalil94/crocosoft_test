import { createSlice } from "@reduxjs/toolkit";

const quizSlice = createSlice({
    initialState: {
        quizes: null
    },
    name: 'quizes',
    reducers: {

    }
})

export default quizSlice.reducer